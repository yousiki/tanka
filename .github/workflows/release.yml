name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g. v0.1.0). Leave empty to use HEAD."
        required: false
        default: ""

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: macos-14
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            arch: aarch64
          - target: x86_64-apple-darwin
            arch: x86_64
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
          key: ${{ matrix.target }}

      - run: bun install --frozen-lockfile

      - name: Build Tanka.app
        run: bun run tauri build --target ${{ matrix.target }}

      - name: Rename DMG artifact
        run: |
          set -euo pipefail
          cd src-tauri/target/${{ matrix.target }}/release/bundle/dmg
          for f in *.dmg; do
            mv "$f" "Tanka-${{ matrix.arch }}.dmg"
          done
          ls -la

      - uses: actions/upload-artifact@v4
        with:
          name: Tanka-${{ matrix.arch }}
          path: src-tauri/target/${{ matrix.target }}/release/bundle/dmg/Tanka-${{ matrix.arch }}.dmg

  release:
    name: Publish release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: Tanka-*
          merge-multiple: true

      - run: ls -la dist

      - name: Generate SHA256 checksums
        run: cd dist && shasum -a 256 *.dmg > SHA256SUMS

      - name: Generate release-ready Homebrew cask
        run: python3 .github/scripts/render_cask.py
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}

      - uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/Tanka-aarch64.dmg
            dist/Tanka-x86_64.dmg
            dist/SHA256SUMS
            dist/tanka.rb
          generate_release_notes: true
